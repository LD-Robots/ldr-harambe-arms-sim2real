<?xml version="1.0"?>
<package format="3">
  <name>rl_locomotion_controller</name>
  <version>0.1.0</version>
  <description>
    ros2_control plugin that runs an ONNX locomotion policy on the Harambe
    humanoid (25 DOF). Outputs position commands in CSP mode. Subscribes to
    pelvis + torso IMU, and cmd_vel. Includes safety: tilt watchdog, rate
    limiting, init ramp from current pose to Isaac default.
  </description>
  <maintainer email="andrei.stroe@ldrobots.ro">LD Robots</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>pluginlib</depend>
  <depend>controller_interface</depend>
  <depend>hardware_interface</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>geometry_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>
  <depend>realtime_tools</depend>
  <depend>ament_index_cpp</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
